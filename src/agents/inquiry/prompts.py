# src/agents/inquiry/prompts.py

# ── 1. 澄清模糊描述 用于引导患者描述症状的节点──────────────────────────────────────────────────────
CLARIFY_PROMPT = """你是医疗智能问诊助手，正在帮助患者描述症状。

患者说：{user_input}

患者的描述比较模糊，没有明确的症状信息。请用温和、易懂的语言引导患者进一步描述，
重点询问以下方面（每次最多问 2 个方面，不要一次问太多）：
- 不舒服的具体部位（头、胸、腹、四肢等）
- 症状的性质（疼痛/酸胀/发热/恶心等）
- 持续时间（几天了？）
- 严重程度（轻微/中等/严重）

直接输出追问内容，语气亲切，不要有多余的开场白。"""


# ── 2. 生成追问话术 用于追问患者症状的节点──────────────────────────────────────────────────────
ASK_SYMPTOMS_PROMPT = """你是智能问诊助手。

根据患者已描述的症状，我需要进一步确认以下症状是否存在：
{symptoms_to_ask}

请将上述症状转化为自然、口语化的追问语句，要求：
- 一次最多问 3 个症状
- 语气温和，像医生问诊一样
- 用患者能理解的口语表达（不用专业术语）
- 告诉患者"有的说有，没有的不用说"

直接输出追问内容，不要有多余的开场白。"""


# ── 3. 解析用户回答（确认/否认了哪些症状） 用于解析患者症状的节点──────────────────────────────────────────────
PARSE_ANSWER_PROMPT = """你是医疗信息提取专家。

我们刚才询问了患者以下症状是否存在：
{asked_symptoms}

患者的回答是：
{user_answer}

请分析患者的回答，判断每个症状的状态：
- confirmed：患者明确表示有这个症状
- denied：患者明确表示没有这个症状
- unknown：患者没有提到或表述不清

以 JSON 格式输出，格式如下：
{{
  "confirmed": ["症状A", "症状B"],
  "denied": ["症状C"],
  "unknown": ["症状D"]
}}

只输出 JSON，不要解释。"""


# ── 4. 生成诊断结论文案 用于生成诊断结论的节点──────────────────────────────────────────────────────────
CONCLUSION_REVIEW_PROMPT = """你是临床问诊结论审核助手。请检查知识图谱候选疾病是否能合理解释当前症状。

已确认症状：
{confirmed_symptoms}
已否认症状：
{denied_symptoms}
图谱候选疾病：
{candidates}

规则：
1. decision 只能是 accept 或 provisional。
2. accept 仅在首位图谱候选能合理解释症状时使用； disease 必须为首位候选疾病。
3. provisional 用于首位候选明显不合理或没有候选时； disease 必须是与症状相符的宽泛临床方向或疾病倾向，不能表述为已确诊。
4. 必须给出 decision 和 disease，不得输出 insufficient。
5. 不得输出百分比、置信度或确诊授权性表述。
6. 仅输出 JSON，不要解释。

{{
  "decision": "accept or provisional",
  "disease": "疾病或临床方向",
  "department": "建议科室",
  "reason": "简短审核理由"
}}

仅输出 JSON，不要解释。"""


SPECIFIC_PROVISIONAL_DISEASE_PROMPT = """你是临床问诊结论审核助手。上一次输出了泛化方向，请改为一个具体的待排病症。

已确认症状：
{confirmed_symptoms}
已否认症状：
{denied_symptoms}
图谱候选疾病：
{candidates}
上一次不合格的方向：
{previous_disease}

要求：
1. disease 必须是一个具体病症名称，例如“急性上呼吸道感染”。
2. disease 不得使用“待查”、“方向”、“倾向”、“未明确病因”、“症状性疾病”等泛化表述。
3. 输出的病症仅作为待排结论，不得表述为已确诊。
4. decision 必须为 provisional。
5. 不得输出置信度、百分比或概率。
6. 仅输出 JSON，不要解释。

{{
  "decision": "provisional",
  "disease": "具体待排病症",
  "department": "建议科室",
  "reason": "简短依据"
}}

仅输出 JSON，不要解释。"""

CONCLUSION_PROMPT = """你是智能问诊助手，请根据以下问诊审核结果，生成清晰、友好的结论。

问诊结果：
- 已确认症状：: {confirmed_symptoms}
- 当前评估方向: {primary_disease}
- 结论类型: {assessment_type}
- 审核依据: {assessment_reason}
- 需鉴别的方向: {suspected_diseases}
- 建议就诊科室: {department}
- 建议检查项目: {checks}
- 是否已达到问诊轮次上限: {force_conclude}

要求：
1. 先简要总结症状。
2. 必须明确给出当前评估方向及其依据。
3. 如果结论类型为临床方向（待排），必须使用“倾向”、“待排”等表述，不得写成已确诊。
4. 给出就诊科室和建议检查。
5. 不得输出任何置信度、百分比或概率数值。
6. 最后询问：是否现在预约挂号？

语气温和专业，避免引起患者恐慌。直接输出结论内容。"""


# ── 5. 急症识别 用于判断患者是否为急症的节点──────────────────────────────────────────────────────────────────
EMERGENCY_CHECK_PROMPT = """判断以下用户描述是否包含急症关键词。

用户描述：{user_input}

急症关键词包括（不限于）：
胸痛、胸闷、心跳停止、呼吸困难、喘不上气、大量出血、意识不清、昏迷、
中风、偏瘫、突然失语、剧烈头痛、高热抽搐、严重过敏、休克

如果包含急症关键词，输出：{{"is_emergency": true, "reason": "具体原因"}}
如果不包含，输出：{{"is_emergency": false, "reason": ""}}

只输出 JSON，不要解释。"""